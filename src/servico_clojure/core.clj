(ns servico-clojure.core)

(println "Started the app")

;(def nome (atom "Cesar"))
;(println @nome)
;
;
;(defn funcao-altera-name
;  [atom novo-nome]
;  (println "Atom:" atom)
;  (println "Novo nome:" novo-nome)
;  novo-nome)
;
;(swap! nome funcao-altera-name "Cesar Augusto")
;(println @nome)

;Alguns atalhos
;shift + alt + l >Executa o código
;shift + alt + p -> Executa apenas uma linha
;shift + alt + l -> Carrega o arquivo no REPL

;(list {:a 100 :b 2000}, {:c 300 :d 4000}, {:e 600 :f 7000})
;(-> body vals first)